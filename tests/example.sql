CREATE TABLE ddl2struct (
    person_id int,
    last_name varchar(255),
    first_name varchar(255),
    address varchar(255),
    city varchar(255)
) comment ' bbb.go';

CREATE TABLE ddl2struct2 (
    person_id bigint,
    last_name varchar(255),
    first_name varchar(255),
    address varchar(255),
    city varchar(255)
) comment ' aaa.go';